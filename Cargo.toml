[package]
name = "twitninf-recommender"
version = "1.0.0"
edition = "2021"

[[bin]]
name = "twitninf-recommender"
path = "src/main.rs"

[dependencies]
# HTTP server
axum = { version = "0.7", features = ["json", "query", "tracing"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "trace", "compression-gzip", "catch-panic"] }

# Async runtime
tokio = { version = "1", features = ["full"] }

# PostgreSQL
tokio-postgres = { version = "0.7", features = ["with-chrono-0_4", "with-serde_json-1", "with-uuid-1"] }
deadpool-postgres = { version = "0.14", features = ["rt_tokio_1"] }

# UUID
uuid = { version = "1", features = ["v4", "serde"] }

# Redis
redis = { version = "0.27", features = ["tokio-comp", "aio"] }
deadpool-redis = { version = "0.15", features = ["rt_tokio_1"] }

# ML / randomness
rand = "0.8"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Logging / tracing
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Error handling
anyhow = "1"

# Date/time
chrono = { version = "0.4", features = ["serde"] }

# Config
dotenvy = "0.15"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
# `panic = "abort"` a été retiré : le service est un serveur multi-tenant, et
# un panic dans le scoring d'UN tweet (indexation, comparaison NaN…) tuait le
# processus entier, donc le feed de tout le monde. Avec unwind, `CatchPanicLayer`
# transforme ce panic en 500 sur la seule requête concernée.
